each page,i in PrimaryNavItems
  if currentPage === page.url
    a(class="fancy-link-brand active", href= page.url + '.html')= page.title
  else
    a(class="fancy-link" href= page.url + '.html')= page.title
